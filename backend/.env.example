MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-host>/<db>?retryWrites=true&w=majority
MONGODB_DB_NAME=roadfix
PORT=3000

# For Vercel, set MONGODB_URI and MONGODB_DB_NAME in Project Settings -> Environment Variables.
# PORT is managed by Vercel and is not required there.
